# Changelog
All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](http://keepachangelog.com/)
and this project adheres to [Semantic Versioning](http://semver.org/).

## [Unreleased]
- Adds first version
- Adds Markdown output via the `markdown` method, the `.md` / `.markdown` extensions on `save`, and the `withImages`, `withoutImages`, `links`, `withoutLinks`, and `keepHeadersAndFooters` options (requires `lit` 2.1 or greater)
